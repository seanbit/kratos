apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{APP_NAME}}-cronjob
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: {{APP_NAME}}-cronjob
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
