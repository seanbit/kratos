apiVersion: apps/v1
kind: Deployment
metadata:
  name: evm-scan-web
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: evm-scan-web
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
